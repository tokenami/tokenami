{
  "name": "tokenami",
  "private": true,
  "license": "MIT",
  "scripts": {
    "prepare": "pnpm build",
    "dev": "pnpm --filter=./examples/remix dev",
    "build": "pnpm -r --filter=./packages/** build"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "typescript": "^5.1.3"
  },
  "engines": {
    "pnpm": ">= 8"
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 100
  }
}
