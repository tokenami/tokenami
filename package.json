{
  "name": "tokenami",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prepare": "pnpm build",
    "dev": "pnpm --filter=./examples/remix dev",
    "build": "pnpm -r --parallel --filter=./packages/** build"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "tsup": "^7.0.0",
    "typescript": "^5.1.3"
  },
  "engines": {
    "pnpm": ">= 8"
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 100
  }
}
