import { css } from './css';

export const srOnly = css({
  '--position': 'absolute',
  '--size': 'var(--size_px)',
  '--margin': 'var(---, -1px)',
  '--overflow': 'hidden',
  '--clip': 'rect(0, 0, 0, 0)',
  '--white-space': 'nowrap',
  '--border-width': 'var(--line-size_0)',
  '--p': 0,
});
